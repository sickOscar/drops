{
    "name": "drops",
    "scripts": {
        "connect": "eval $(ssh-agent) && ssh -i ~/.ssh/citd.pem ec2-user@54.228.93.106"
    },
    "devDependencies": {
        "@types/node": "^14"
    },
    "dependencies": {
        "@pulumi/pulumi": "^3.0.0",
        "@pulumi/aws": "^5.0.0",
        "@pulumi/awsx": "^0.40.0"
    }
}
